import { CategoryRecommendationsScreen } from "@/screens/recommendations/CategoryRecommendationsScreen";

export default function HomeScreen() {
  // Expo Router requires default export for the `(tabs)/index` screen.
  return <CategoryRecommendationsScreen />;
}

